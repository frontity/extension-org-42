import Store from './stores/client';
import env from './env';
import { version } from '../../package.json';

const ExtensionOrg42 = () => null;

export default ExtensionOrg42;
export { Store, env, version };
